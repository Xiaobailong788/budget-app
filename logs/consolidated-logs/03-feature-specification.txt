# Feature Specification — Budget App

## 1. Architecture Overview

The entire application is a single self-contained HTML file (~6,720 lines) with zero external dependencies.

| Layer | Lines | Description |
|---|---|---|
| CSS Design System | ~1,050 | CSS variables, layout, dark mode, animations, responsive, print styles |
| Default Categories | ~40 lines | 8 root categories with child subcategories, emoji icons, colors |
| DataStore | ~300 lines | localStorage CRUD for records, categories, budgets, savings target |
| StatsEngine | ~160 lines | Aggregation, prediction, filtering logic |
| Excel Export | ~460 lines | XML Spreadsheet 2003 generator with formulas |
| HTML Structure | ~60 lines | 7 page sections + sidebar + bottom nav |
| Render Functions | ~1,200 lines | Per-page renderers (overview, add, records, categories, stats, report, settings) |
| Chart Drawing | ~900 lines | Canvas-based pie, line, bar, ring, sparkline, savings, comparison charts |
| UI Helpers | ~200 lines | Toast, modal, navigation, theme toggle, format helpers |
| Initialization | ~50 lines | Hash routing, resize handling, roundRect polyfill |

### Navigation (7 tabs)

| Tab ID | Chinese Label | Icon | Render Function |
|---|---|---|---|
| `overview` | 总览 | chart | `renderOverview()` |
| `add` | 记账 | pen/plus | `renderAddPage()` |
| `records` | 流水 | clipboard | `renderRecords()` |
| `categories` | 分类 | tag | `renderCategories()` |
| `stats` | 统计 | chart-up | `renderStats()` |
| `report` | 报告 | clipboard | `renderReport()` |
| `settings` | 设置 | gear | `renderSettings()` |

Responsive: Sidebar (desktop >=1024px) + bottom tab nav (mobile). Hash-based routing via `location.hash`.

---

## 2. Data Layer (DataStore)

`const DataStore = { ... }` — a singleton object managing all persistent data via `localStorage`.

### Data Schema

```javascript
{
  records: [
    { id, amount, categoryId, date, note, createdAt, updatedAt }
  ],
  categories: [
    { id, name, icon, color, parentId (null|string), sortOrder }
  ],
  budgets: {
    "2026-07": 3000   // monthly total budget (RM)
  },
  categoryBudgets: {
    "cat-root-1:2026-07": { value: 500, type: "fixed"|"percent" }
  },
  savingsTarget: {
    type: "fixed" | "percent" | "both",
    fixedAmount: 200,
    percent: 10
  },
  colorIndex: 8
}
```

### Key Methods

| Method | Description |
|---|---|
| `init()` | Load from localStorage; migrate missing fields |
| `save()` | Persist to localStorage |
| **Records** | |
| `getRecords()` / `getRecord(id)` | Get all / single |
| `addRecord(record)` | Prepend with UUID |
| `updateRecord(id, updates)` | Patch and set updatedAt |
| `deleteRecord(id)` | Immediate permanent delete |
| `softDeleteRecord(id)` | Remove from list + 5s timer (undo window) |
| `undoDelete()` | Restore pending-delete record |
| **Categories** | |
| `getCategories()` / `getCategory(id)` | Get all / single |
| `getRootCategories()` | Filter `!parentId`, sorted by sortOrder |
| `getChildren(parentId)` | Direct children, sorted |
| `getDescendantIds(id)` | Recursive descendant IDs |
| `addCategory(cat)` | With auto-color assignment |
| `updateCategory(id, updates)` | Patch |
| `deleteCategory(id, options)` | With moveChildren or deleteChildren strategies |
| **Budgets** | |
| `getBudget(month)` / `setBudget(month, amount)` | Total monthly budget |
| `getCategoryBudget(catId, month)` | Returns `{value, type}` object |
| `setCategoryBudget(catId, month, amount, type)` | Supports 'fixed' or 'percent' |
| `getAllCategoryBudgets()` | Raw dict |
| **Savings** | |
| `getSavingsTarget()` / `setSavingsTarget(target)` | Config object |
| **Export** | |
| `exportJSON()` / `importJSON(jsonStr, mode)` | Replace or merge |
| `exportCSV()` | UTF-8 BOM CSV |
| `clearAll()` | Reset to defaults |

### Notable: Soft Delete with Undo (5-second window)

Records are not immediately deleted. `softDeleteRecord(id)` removes the record from the active list but holds it in a `_pendingDelete` buffer for 5 seconds. During that window, `undoDelete()` restores it. If a second delete occurs within the window, the previous pending delete is finalized immediately. Uses `setTimeout` with 5000ms delay.

---

## 3. Statistics Engine (StatsEngine)

`const StatsEngine = { ... }` — pure computation, no side effects.

| Method | Description |
|---|---|
| `getRecordsInMonth(month)` | Filter records by month key |
| `getMonthTotal(month)` | Sum of all amounts |
| `getCategoryTotals(month)` | `{ catId: total }` map |
| `getDailyTotals(month)` | Array `[{day, total}]` for all days of month |
| `getDailyAverage(month)` | Month total / days passed |
| `getPredictedTotal(month)` | Daily avg x total days in month |
| `getSavingsPrediction(month)` | Budget - predicted total |
| `getRemainingDailyLimit(month)` | (Budget - spent) / remaining days |
| `getCategoryBreakdownDeep(month, catId)` | Recursive breakdown with children |
| `getCustomRangeTotals(startDate, endDate)` | Full analysis for custom date range |
| `getOverspentCategories(month)` | Categories exceeding 80% of budget |
| `getLast7Days()` | `[{date, total, label}]` |
| `getMonthlyTotals(N)` | Last N months of aggregated totals |

---

## 4. Page: Overview (总览)

**Tab:** `overview` | **Function:** `renderOverview()`

### Sections (top to bottom):

| Section | Description |
|---|---|
| **Empty State** | Shown when no records exist — "Welcome" card with dashed border, gradient bg, "记第一笔账" CTA button |
| **Grid-4 Summary Cards** | 本月总支出 (month total), 预算 (budget, or "未设置"), 日均支出 (daily avg), 预测月总支出 (predicted total) |
| **Budget Progress Ring** | Canvas donut chart showing % of spendable budget consumed. Shows overspend in red. Below: spendable budget equation (budget - savings = disposable) |
| **Savings Target Ring** | Canvas donut chart showing % of savings target achieved. Green if met, amber if in progress, gray if unset |
| **Savings Prediction Card** | Detailed breakdown: current saved, target, predicted end-of-month savings, % goal achieved, budget surplus. Includes two daily limit indicators: gross (no savings deduction) and net (after savings). Smart message: positive or negative prediction |
| **Last 7 Days Sparkline** | Small canvas line chart showing daily totals |
| **Mo Bi (相比上月)** | Month-over-month change percentage |
| **Top 5 Spending Categories** | Ranked list with numbered badges, emoji, budget status badges (超支/预算内), spends as RM + % |
| **Budget Progress Card** | Reusable component with solid/segmented toggle, sort, monitor selection |
| **Overspend Warning** | Red-bordered card showing categories that exceed 80% of budget |
| **Quick Actions** | Two buttons: "记一笔" (add) and "查看统计" (stats) |

### Notable Features
- Dual daily remaining limits (gross vs. net-of-savings)
- Spendable budget concept: budget - savings = what you can actually spend
- All four metrics (ring, prediction, daily limits, top 5) interconnected around savings target

---

## 5. Page: Add Record (记一笔)

**Tab:** `add` | **Function:** `renderAddPage()`

### Form Fields

| Field | Type | Details |
|---|---|---|
| Amount | Text input | RM prefix, input validation (numbers + 2 decimals only), auto-filters non-numeric |
| Category | Button -> Modal | Opens tree category picker; selected displays color dot + icon + name |
| Date/Time | `datetime-local` | Defaults to current local time with timezone offset |
| Note | Text input | Max 200 characters, placeholder "例：椰浆饭" |
| Submit | Button | Validates amount > 0 and category selected; `shakeForm()` animation on error; resets all fields on success |

### Notable Features
- Real-time amount formatting (strips non-numeric, limits to 2 decimal places)
- Shake animation on validation failure (0.5s CSS keyframe)
- Category picker is a reusable tree modal (used in 7+ contexts)

---

## 6. Page: Records (流水)

**Tab:** `records` | **Function:** `renderRecords()`

### Search & Filter Bar

| Filter | Type | Details |
|---|---|---|
| Keyword search | Text input | Searches `note` field (case-insensitive) |
| Category filter | Button -> Modal | Tree picker with "全部分类" option; filters by descendant IDs |
| Date start/end | Date inputs | Filters `date` or `createdAt` |
| Amount min/max | Number inputs | Range filter |
| Overspent only | Toggle button | Shows only records from categories that exceeded budget |

Filter bar action row: Clear filters, Overspent toggle, Batch mode toggle, Export Excel.

### Display Controls

| Control | Details |
|---|---|
| View toggle | Compact view (single line) vs Card view (full detail) — persisted in localStorage |
| Batch mode toggle | Enter/exit multi-select mode |
| Page size selector | Configurable 5-200 records per page, persisted in localStorage |
| Record count | Shows total filtered count, e.g. "128 条记录" |

### Record List

| Feature | Details |
|---|---|
| Pagination | First (<<), prev (<), page numbers (max 7), next (>), last (>>), current/total page count |
| Compact view | Single-line: date + icon + name + note + amount + delete button |
| Card view | Full card: icon, name (full path), note, amount, date/time, edit button |
| Record selection (batch mode) | Checkbox per record, visual highlight |
| Edit | Opens modal with amount, category, date, note pre-filled |
| Single delete | Modal confirmation -> soft delete with 5s undo toast |
| Undo | "撤销" button in toast; restores the record via `DataStore.undoDelete()` |
| Double-delete guard | If same record deleted again within 5s window, timer resets |

### Batch Operations Toolbar

Appears at bottom when batch mode is active:

| Operation | Details |
|---|---|
| Batch delete | Confirmation modal -> soft deletes all selected (each with own 5s undo) |
| Batch change category | Opens tree picker -> updates all selected records' categoryId |
| Cancel | Clears selection and exits batch mode |

### Notable Features
- Soft delete with undo (5-second window)
- Overspent-only filter
- Batch operations (delete + recategorize)
- Configurable page size persisted in localStorage
- Compact/card view toggle persisted in localStorage

---

## 7. Page: Categories (分类)

**Tab:** `categories` | **Function:** `renderCategories()`

### Structure

- Accordion tree view with expand/collapse (arrow toggle)
- Root nodes start collapsed (no auto-expand)
- Shows total category count
- "添加根分类" button at bottom
- "展开全部" / "折叠全部" quick actions in header

### Per-Category Actions

| Action | Details |
|---|---|
| Expand/Collapse | Toggle arrow icon; state tracked in `expandedCategories` Set |
| Add child | + button in action icons; prompts for name via modal |
| Edit (via gear icon) | Opens edit modal with rename, icon, color, move, merge, delete |
| Rename | Via edit modal: text input for name |
| Change icon | Via edit modal: "更改图标" button opens emoji grid modal (~100 emoji scrollable grid) |
| Change color | Via edit modal: "更改颜色" button opens 14 preset color swatches + custom HEX input |
| Move | Via edit modal: tree picker to choose new parent (with cycle detection) |
| Merge | Via edit modal: merge into another category (records reassigned, child handling options) |
| Delete | Via edit modal: options: move children to parent, delete children, or cancel if has children |

### Per-Category Budget

| Feature | Details |
|---|---|
| Input field | Inline number input directly in the tree header row (always visible, collapsed or expanded) |
| Unit toggle | Click RM / % to toggle between fixed amount and percentage |
| Percentage budgets | Computed as % of total monthly budget |
| Validation | Child budget sum cannot exceed parent budget — shows warning toast and rejects save |
| Leaf child budgets | Separate budget input row below card for leaf categories (smaller dimensions) |

### Header Row Layout (in order)

1. `cat-arrow` / `cat-arrow-empty` (conditional on hasChildren)
2. `cat-dot` (colored circle)
3. `cat-icon` (emoji)
4. `cat-name` (text)
5. `cat-action-icons` (+ and gear icons)
6. `cat-budget` (input + toggle + unit, right-aligned via `margin-left: auto`)

### Category Merge

| Feature | Details |
|---|---|
| Source category selected | Shows modal with target tree picker |
| All records reassigned | Records of source + descendants moved to target |
| Child handling | Option to move children under target, or delete all children |
| Source deleted | After merge, source category is removed |

### Notable Features
- Inline RM/% toggle for budgets
- Parent-child budget validation
- Category merge with child handling options
- Tree-based picker reused across add, filter, merge, batch, move operations
- Breadcrumb at depth >= 4
- Deep nesting support (infinite levels)

---

## 8. Page: Statistics (统计)

**Tab:** `stats` | **Function:** `renderStats()`

### Date Selection

| Feature | Details |
|---|---|
| Month selector | `<input type="month">` |
| Custom date range | Two date inputs; auto-swaps if start > end; overrides month selection |
| Custom range analysis | Full stats for arbitrary date periods |
| Modes are mutually exclusive | Selecting month clears custom range and vice versa |

### Summary Cards (Row 1 — 4-grid)

| Card | Details |
|---|---|
| Total expense | Month total or custom range total |
| Daily avg | Month daily avg, or record count for custom range |
| Predicted total | Projected end-of-month total |
| Savings prediction | Budget - predicted total; color-coded green/red |

### Transaction Analysis Cards (Row 2 — 4-grid)

| Card | Details |
|---|---|
| Transaction count | Total count + days with transactions |
| Average per transaction | Total / count |
| Max single transaction | Highest amount + its category |
| Max spending day | Highest daily total + date |

### Savings Estimation Card (month only)

Shows: budget, current saved, predicted savings, surplus. Dual daily limits (gross/net). Visible only for current month.

### Charts & Visualizations

| Chart | Type | Details |
|---|---|---|
| Calendar Heatmap | HTML grid | 6-color gradient based on spending ratio vs daily target. Legend, weekday headers, clickable cells -> popup with day's records |
| Pie Chart | Canvas | Interactive: hover pop-out (8px shift + shadow + white border), click-to-drill-down. Labels with semi-transparent backgrounds. Legend with budget progress bars per category |
| Line Chart | Canvas | Animated 800ms ease-out cubic draw. Bezier curve interpolation, gradient fill, savings target dashed reference line (green), dot markers |
| Budget Progress Card | HTML component | Same component as overview (solid/segmented toggle, sort, monitor selection) |
| Month-Over-Month Comparison | Canvas | Toggleable grouped bar chart (current month blue, previous month green) |
| Monthly Spending Trend | Canvas | 6-month line chart with dots, labels, gradient fill |
| Monthly Savings Trend | Canvas | 6-month bar chart: green bars for positive savings, red for negative, zero line |

### Interactive Features

| Feature | Details |
|---|---|
| Pie drill-down | Click a slice -> dive into subcategories. Breadcrumb navigation. "返回上级" button. Drill stack managed globally |
| Chart expand | Expand button on heatmap and pie charts -> opens centered modal with dimmed backdrop, larger view |
| Chart PNG download | Each chart has "下载 PNG" button |
| Heatmap day click | Click any day cell -> shows records for that date (popup inline list) |
| Bar chart drill-down | Click a bar -> drill into subcategories |
| Pie and bar share drill state | Drilling in pie also updates bar chart and vice versa |
| Date change resets drill | Changing month or custom range resets all drill-down states |

### Calendar Heatmap Details

- HTML CSS Grid, 7 columns (Sun-Sat)
- 6-color gradient: 0 (no spend) = light gray; 0-0.4 = dark blue; 0.4-0.7 = cyan; 0.7-1.0 = green; 1.0 = warm yellow; 1.0-1.5 = orange; 1.5+ = deep red
- Future dates: semi-transparent and non-clickable
- Hover tooltip: shows specific date and spending amount
- Color legend bar from "少" to "多"
- Click interaction: shows day's record list with edit/delete capability
- Expandable to modal view

### Pie Chart Details (expand)

- Elastic spring animation using `requestAnimationFrame`, 600ms, custom easing (1 - pow(2, -7x) * cos(x * PI * 2 * 1.2))
- Radius animates from 30% to 100%
- Hover pop-out: slice shifts 8px with shadow + white border; color deepens 18%
- Click drill-down: click slice -> subcategory distribution; or leaf -> close overlay if in expanded mode
- Labels: semi-transparent white background, positioned radially outside slices
- Drillable labels have underline
- Legend: color boxes + names + amounts + budget progress micro-bars (80px, green < 80%, yellow 80-100%, red > 100%)
- Touch support: `touchstart`/`touchend` mapped to mousemove/click
- No-animation mode for expand modal
- No-data state: centered "暂无数据" text

### Line Chart Details (expand)

- 800ms ease-out cubic animation (1 - (1-t)^3)
- Bezier curve interpolation (smooth curves)
- Gradient fill below line using `createLinearGradient` (semi-transparent to transparent)
- Savings target dashed reference line (green with label)
- Dot markers: solid circles with white stroke
- Grid: 4 horizontal lines + left Y-axis labels
- X-axis labels: auto-spaced based on data density

### Bar Chart Details

- Horizontal bars with rounded top corners (`roundRect`)
- Color-coded by category's assigned color
- Value labels above bars
- Name labels below bars (truncated at 4 characters with "..")
- Budget progress micro-bar (4px height, green/yellow/red) under name label
- Click -> drill-down (pushes to drill stack)

### Month-Over-Month Comparison Chart

- Grouped bars: each category has two side-by-side bars
- Current month (blue), previous month (green)
- Value labels on each bar
- Legend: "蓝:本月 / 绿:上月"

### Monthly Spending Trend Chart

- 6 months of aggregated totals
- Line with gradient fill, dot markers, value labels above dots
- X-axis: "X月" format labels

### Monthly Savings Trend Chart

- 6 months of savings/overspend bars
- Green bars (positive savings), red bars (negative)
- Dashed zero line
- Value labels at bar tops/bottoms

### Notable Features
- Full interactive pie chart with drill-down, hover pop-out, budget progress in legend
- Calendar heatmap with spending ratio coloring
- Custom date range support
- Dual daily limit display (gross + net-of-savings)
- Month-over-month comparison toggle
- Every chart downloadable as PNG
- Pie and bar share drill state
- Dark mode chart adaptation via `getThemeColors()`

---

## 9. Page: Report (月度报告)

**Tab:** `report` | **Function:** `renderReport()`

### Print-Optimized Layout

| Feature | Details |
|---|---|
| Month selector | Input to pick report month |
| Print button | Triggers `window.print()` with print-specific CSS |

### Summary Cards (4-grid)

| Card | Details |
|---|---|
| Budget | Monthly allowance (or "未设置") |
| Total expense | Color-coded green/red vs spendable budget |
| Savings | Positive/negative |
| Savings rate | % of budget saved; green >= 20%, amber > 0%, red otherwise |

### Rings & Visuals

| Visual | Details |
|---|---|
| Budget progress ring | Canvas donut chart |
| Savings target ring | Canvas donut chart |
| Daily trend sparkline | Small canvas line chart |

### Category Breakdown Table

| Column | Details |
|---|---|
| Category | Icon + name |
| Budget (RM) | Per-category budget |
| Expense (RM) | Amount spent |
| Ratio | % of total spending |
| Remaining (RM) | Budget - spent, color-coded |
| Status | 预算内 / 超支 / -- |

Includes total row and top-5 spending ranking.

### Savings Prediction Summary

Narrative text block: "Budget is RM X. Savings target is RM Y. Disposable is RM Z. Spent so far is RM A. If current habits continue, predicted end-of-month: surplus/deficit."

### Print CSS

- Hides navigation, buttons, non-report elements
- Report cards get `break-inside: avoid`
- Full-width layout, no shadows, white backgrounds
- Canvas auto-width

### Notable Features
- Print-optimized report page
- Narrative savings prediction in plain language
- Complete category breakdown table with status indicators

---

## 10. Page: Settings (设置)

**Tab:** `settings` | **Function:** `renderSettings()`

### Sections

| Section | Details |
|---|---|
| Theme Toggle | Dark/light mode button. Toggles `data-theme` attribute on `<html>`. Persisted via localStorage |
| Monthly Budget | Month input + amount input + save button. The cornerstone of all budget/savings calculations |
| Savings Target | Type selector: fixed amount, % of budget, or both. Dynamic inputs based on type. Save button |
| Data Management | Export JSON, Import JSON (replace or merge), Export Excel, Export CSV, Clear all data |

### Savings Target Configuration

| Mode | Calculation |
|---|---|
| `fixed` | User enters a fixed RM amount to save |
| `percent` | % of monthly budget (e.g., 10% of RM3000 = RM300) |
| `both` | Fixed + percent combined (e.g., RM200 + 10% = RM500) |

Each mode shows only the relevant input fields (dynamic show/hide).

### Data Management

| Operation | Format | Details |
|---|---|---|
| Export JSON | `.json` | Filename: `budget-data-YYYY-MM-DD.json` |
| Import JSON | `.json` | File picker -> modal: "替换当前数据" or "合并到当前数据" |
| Replace mode | — | Overwrites all existing data |
| Merge mode | — | Appends records, adds new categories, merges budgets and savings |
| Export Excel | `.xlsx` | XML Spreadsheet 2003, 5 sheets with live formulas |
| Export CSV | `.csv` | UTF-8 BOM, columns: ID, Amount, Category, Date, Note, CreatedAt |
| Clear all data | — | Confirmation modal -> resets to defaults |

### Notable Features
- Three-mode savings target (fixed/percent/both)
- Import with replace vs. merge options
- JSON/CSV/Excel multi-format export
- Clear all data with confirmation

---

## 11. Charting & Visualization

All charts are Canvas-based, drawn with `getContext('2d')`, with DPR-aware sizing and CSS-driven responsive width.

### `drawRing(canvasId, progress, color, label, overspendColor)`
- Donut chart for budget/savings progress
- Animated via requestAnimationFrame (ease-out)
- Text label in center
- Overspend mode (red color, can exceed 100%)
- Three state colors: green (normal), amber (warning), red (overspend)
- `lineCap: 'round'` for rounded endpoints

### `drawSparkline(canvasId, data)`
- Mini line chart for daily trends
- Optional trend color (green/red based on direction)
- Compact, no axes
- Line + gradient fill + dot markers

### `drawPieChart(canvasId, month, startDate, endDate, onDrill, height, noAnim)`
- Animated elastic pie chart (spring-like bounce via requestAnimationFrame, 600ms)
- Hover pop-out: slice shifts 8px with shadow + white border, color deepens 18%
- Click-driven drill-down: click slice -> drill into subcategories
- Labels: text with semi-transparent white background, position clamped
- Legend: color boxes + names + amounts + budget progress bars (80px wide)
- Touch support: `touchstart`/`touchend` mapped to mousemove/click
- No-animation mode for expand modal
- `roundRect` polyfill for older browsers
- No-data state: centered "暂无数据"
- Hover: cursor changes to pointer on drillable slices

### `drawLineChart(canvasId, month, startDate, endDate)`
- Animated 800ms ease-out cubic draw (1 - (1-t)^3)
- Bezier curve interpolation (smooth curves, not straight segments)
- Gradient fill below the line (`createLinearGradient`)
- Savings target dashed reference line (green)
- Dot markers at data points (white stroke)
- Grid lines + axis labels (4 horizontal lines)
- X-axis labels auto-spaced

### `drawBarChart(canvasId, month, startDate, endDate)`
- Horizontal category bars with rounded top corners (`roundRect`)
- Color-coded by category color
- Value labels above bars
- Name labels below bars (truncated at 4 chars with "..")
- Budget progress micro-bar (4px) under each name label
- Click -> drill-down (pushes to drill stack)

### `drawMonthlyChart(canvasId)`
- 6-month spending trend line chart
- Gradient fill + dot markers + value labels
- X-axis: "X月" format

### `drawSavingsChart(canvasId)`
- 6-month savings trend bar chart
- Green bars (positive savings) / Red bars (negative)
- Zero line, grid, value labels

### `drawCompareBarChart(canvasId, month)`
- Grouped bar chart: current month (blue) vs previous month (green)
- Per-category side-by-side bars
- Legend: "蓝:本月 / 绿:上月"
- Value labels on bars

### `downloadChart(canvasId)`
- Converts canvas to PNG data URL
- Triggers download with filename `{canvasId}.png`

### Common Chart Features
- **DPR awareness:** All canvases use `window.devicePixelRatio` for high-DPI display
- **CSS responsive width:** Canvas uses CSS `width: 100%` with internal height control
- **Dark mode adaptation:** `getThemeColors()` reads CSS variables (`--text-primary`, `--text-secondary`, `--text-muted`, `--border`, `--bg`, `--card-bg`)
- **setTransform:** Used instead of `scale` to prevent transform matrix accumulation
- **roundRect polyfill:** For browsers not supporting `CanvasRenderingContext2D.roundRect`
- **No-hover-tooltips:** Click-based drill-down instead of hover tooltips (design choice)

---

## 12. Export / Import

### JSON Export/Import
- `exportJSON()`: Downloads `budget-data-{date}.json`
- `importJSON(event)`: File reader -> shows modal with "替换当前数据" or "合并到当前数据"
  - Replace: Overwrites all data
  - Merge: Appends records, adds new categories, merges budgets and savings target

### CSV Export
- `exportCSV()`: UTF-8 BOM CSV with columns: ID, Amount, Category, Date, Note, CreatedAt
- Downloads `budget-records-{date}.csv`
- Note content with double quotes auto-escaped

### Excel Export (XML Spreadsheet 2003)
- No ZIP/library required — generates raw XML SpreadsheetML
- Native open in Excel / LibreOffice / Google Sheets
- **5 sheets with live formulas:**

| Sheet | Content | Formulas |
|---|---|---|
| 1 — 消费记录 | All records with totals | `=SUM()`, `=AVERAGE()`, `=MAX()`, `=MIN()`, `=COUNT()` |
| 2 — 分类统计 | Category breakdown | `=SUMIF()`, `=COUNTIF()`, `=IF(..., /, 0)` |
| 3 — 月度统计 | Per-month summary: budget, savings target, disposable, actual, remaining, usage rate | `=SUMIF()`, `=AVERAGEIF()`, custom calculations |
| 4 — 预算跟踪 | Monthly budget tracking with status | `=IF(RC[-1]>=0, "正常", "超支")` |
| 5 — 储蓄统计 | Monthly savings breakdown | `=MIN(1, MAX(0, RC[-1]/RC[-4]))` |

- All formulas are live — users can modify values and recalculations happen automatically
- Styled headers (blue/green), currency formatting, percentage formatting, frozen headers
- Auto-filter enabled on all sheets
- UTF-8 encoding

---

## 13. UI/UX Features

### Design System
- CSS custom properties for theming (colors, shadows, radii, transitions)
- Indigo primary palette (#6366F1) with emerald, amber, rose accents
- Consistent card-based layout with `--card-bg`, `--shadow`, `--radius`
- Smooth transitions with cubic-bezier easing

### Dark Mode
- Full dark theme via `[data-theme="dark"]` CSS overrides
- All color variables redefined: backgrounds, cards, text, borders, shadows
- Persisted in localStorage
- Re-renders settings page on toggle to update button text
- All canvas charts read `getThemeColors()` for dynamic text/border colors

### Responsive
- Desktop (>=1024px): Sidebar (220px) + main content
- Tablet (>=480px): Larger text, grids adapt
- Mobile: Bottom tab navigation (64px height), `safe-area-inset-bottom` support
- Canvas charts use DPR-aware sizing with CSS `width:100%`

### Animations
| Animation | Duration | Easing | Trigger |
|---|---|---|---|
| `pageEnter` | 0.35s | cubic-bezier | Tab switch |
| `slideInLeft` | 0.3s | ease | Sidebar |
| Toast fade+slide | 3s | spring cubic-bezier | Auto-dismiss |
| Modal overlay | 0.25s | ease | Modal open/close |
| Modal content | 0.3s | spring overshoot | Modal open |
| Chart expand fade | 0.25s | ease | Expand button |
| Chart expand slide | 0.25s | ease | Expand button |
| Shake form | 0.5s | — | Validation failure |
| Budget progress fade | 0.35s | ease | Data change |
| Pie chart draw | 600ms | elastic spring | Page render |
| Line chart draw | 800ms | ease-out cubic | Page render |
| Ring draw | ~500ms | ease-out | Page render |
| Accordion expand | 0.35s | cubic-bezier(0.4,0,0.2,1) | Arrow click |

### Components
| Component | Usage |
|---|---|
| Toast | Success/error/warning/info notifications, 3s auto-dismiss, close button, slide-in animation |
| Modal | Category pickers, confirmations, edit forms, import selection |
| Category Tree Picker | Reusable in 7+ contexts (add, edit, filter, batch, merge, move, icon picker) |
| Budget View Toggle | Slider-style toggle (solid/segmented) |
| Pagination | First/prev/pages/next/last, max 7 visible page buttons |
| Badge | Success (预算内) / Danger (超支) |
| Card | Consistent container with title, shadow, rounded corners |
| Input Group | Label + styled input |
| Button | Primary, outline, ghost, danger; multiple sizes |
| Color Picker | 14 preset swatches + custom HEX input |
| Emoji Picker | ~100 emoji scrollable grid |

---

## 14. Notable / Unique Features

These 24 standout capabilities differentiate the app from typical budget trackers:

| # | Feature | Page | What makes it special |
|---|---|---|---|
| 1 | **Elastic Animated Pie Chart** | Stats | Spring-bounce animation on draw, hover pop-out with shadow + white border, click-drill-down, labels with backgrounds, legend with budget progress bars — all in pure Canvas |
| 2 | **Budget Progress Card (Solid/Segmented)** | Overview, Stats, Categories | Toggle between single-color bars and segmented bars showing child-category breakdown visually within each parent bar |
| 3 | **Budget Monitor Selection** | Overview, Stats | Choose which budgeted categories to display (checkbox UI with select all/invert), persisted in localStorage |
| 4 | **Three-Mode Savings Target** | Settings | Fixed amount, % of budget, or both combined — feeds into spendable budget concept |
| 5 | **Spendable Budget Concept** | Overview, Stats, Report | Budget - savings target = disposable income. All metrics (daily limits, progress rings, predictions) use this spendable amount |
| 6 | **Calendar Heatmap** | Stats | Full month grid coloring each day's spending vs. daily target (6-color gradient). Click any day -> see that day's records |
| 7 | **Interactive Pie Drill-Down** | Stats | Click pie slice to drill into subcategories with breadcrumb navigation. Works in both inline and expanded modal views |
| 8 | **Month-Over-Month Comparison** | Stats | Toggle grouped bar chart showing each category this month vs last month |
| 9 | **Soft Delete with Undo** | Records | 5-second grace window to undo any deletion. Only one pending delete at a time |
| 10 | **Compact/Standard Record View** | Records | Toggle persisted in localStorage; compact shows single-line, standard shows full card |
| 11 | **Child Budget Percentage Support** | Categories | Sub-category budgets can be set as % of total monthly budget with parent-child budget validation |
| 12 | **Category Merge** | Categories | Merges source category into target with all records reassigned. Options: move children or delete them |
| 13 | **Excel Export with Live Formulas** | Settings | Real XML Spreadsheet with SUMIF, IF, AVERAGE formulas that auto-recalculate. No library needed. 5 sheets |
| 14 | **Consistent Category Tree Picker** | Add, Records, Categories | Same tree-based modal used everywhere — add record, edit, filter, batch, merge, move. 7+ reuse contexts |
| 15 | **Custom Date Range Analysis** | Stats | Beyond monthly view — full stats for any date range with totals, counts, daily breakdown |
| 16 | **PNG Chart Download** | Stats | Every chart (pie, line, monthly, savings, comparison) has a "下载 PNG" button |
| 17 | **Dark Mode** | Settings | Full dark theme with CSS custom properties, all chart colors adapt |
| 18 | **Responsive Dual Navigation** | Global | Desktop sidebar + mobile bottom tab nav with safe-area support |
| 19 | **Dual Daily Limits** | Overview, Stats | Shows both "gross" (budget-only) and "net" (budget - savings) daily remaining limits |
| 20 | **Overspent Filter** | Records | Filter to show only records belonging to categories that exceeded their budget |
| 21 | **Batch Operations** | Records | Multi-select records for bulk delete or bulk category change |
| 22 | **Printer-Friendly Report** | Report | Print CSS that hides nav/buttons, optimizes layout, preserves `break-inside: avoid` |
| 23 | **Savings Prediction Narrative** | Overview, Report | Natural language summary: positive or negative prediction based on current spending habits |
| 24 | **5-Sheet Excel Export** | Settings | 5 interconnected sheets: records, category stats, monthly summary, budget tracking, savings stats — all with live formulas |

---

## Appendix A: localStorage Keys Reference

| Key | Purpose | Type |
|---|---|---|
| `budgetAppData` | All application data (records, categories, budgets, settings) | JSON string |
| `budgetTheme` | Dark/light theme preference | `'light'` or `'dark'` |
| `budgetProgressSort` | Budget progress card sort method | `'usage'` / `'amount'` / `'name'` |
| `budgetProgressView` | Budget progress card view mode | `'solid'` / `'segmented'` |
| `budgetMonitoredIds` | Monitored category ID list | JSON array or null |
| `budgetCompactView` | Records page compact view toggle | `true` / `false` |
| `budgetRecordsPerPage` | Records per page setting | number string |

## Appendix B: Default Category Structure

| Root Category | Sub-categories |
|---|---|
| 餐饮 | 早餐, 午餐, 晚餐, 饮料/咖啡 |
| 交通 | 油费, 停车, 公交/地铁, 打车 |
| 购物 | 日用品, 服饰, 电子产品 |
| 娱乐 | 电影, 游戏, 运动 |
| 居住 | 房租, 水电, 网络 |
| 医疗 | 看病, 药品 |
| 教育 | 书籍, 课程 |
| 其他 | (none) |

## Appendix C: Complete Function Index

| Function | Line (approx.) | Purpose |
|---|---|---|
| `escHtml(str)` | 762 | Sanitize HTML output |
| `uuid()` | 1154 | Generate unique ID |
| `getMonthKey(dateStr)` | 1158 | Extract YYYY-MM from date string |
| `DataStore.*` | 1166 | Data persistence layer (30+ methods) |
| `exportToExcel()` | 1465 | Generate XML Spreadsheet |
| `navigateTo(tab)` | 2100 | Tab routing + render |
| `applyTheme(theme)` | 2142 | Dark/light mode |
| `toggleTheme()` | 2147 | Toggle dark mode |
| `showToast(msg, type)` | 2156 | Toast notification |
| `showModal(html)` / `closeModal()` | 2171 | Modal dialog |
| `formatMoney(amount)` | 2185 | Format as RM 1,234.56 |
| `getCategoryFullPath(catId)` | 2189 | Icon Name > Parent > Child |
| `getRootAncestorId(catId)` | 2199 | Top-level ancestor ID |
| `getRootAncestor(catId)` | 2207 | Top-level ancestor object |
| `renderOverview()` | 2218 | Overview page |
| `getThemeColors()` | 2440 | Read CSS vars for canvas |
| `loadBudgetMonitored()` | 2457 | Load monitor preferences |
| `refreshBudgetCards(month)` | 2468 | Refresh budget progress |
| `toggleBudgetView(month)` | 2475 | Solid/segmented toggle |
| `showBudgetSelector(month)` | 2495 | Category monitor picker |
| `confirmBudgetSelection(month)` | 2544 | Save monitor selection |
| `renderBudgetProgressCard(month)` | 2565 | Budget progress card (wrapper) |
| `renderBudgetProgressCardInner(month)` | 2644 | Budget progress card (inner) |
| `drawRing(...)` | 2792 | Donut chart on canvas |
| `drawSparkline(...)` | 2844 | Sparkline chart on canvas |
| `renderAddPage()` | 2906 | Add record page |
| `openCategoryPicker(context)` | 2964 | Open category tree modal |
| `buildCategoryTreePicker(cats, depth, context)` | 2972 | Tree picker HTML |
| `selectCategory(catId, context)` | 2993 | Pick category from tree |
| `submitRecord(e)` | 3011 | Save new record |
| `renderRecords()` | 3070 | Records list page |
| `toggleRecordsView()` | 3151 | Compact/card toggle |
| `getFilteredRecords()` | 3157 | Apply all filters |
| `toggleOverspentFilter()` | 3220 | Toggle overspent filter |
| `applyRecordsFilter()` | 3226 | Apply filter changes |
| `openCategoryFilterPicker()` | 3236 | Category filter modal |
| `clearRecordsFilter()` | 3286 | Clear all filters |
| `renderRecordsList()` | 3291 | Render paginated records |
| `toggleBatchMode()` | 3458 | Enter/exit batch mode |
| `toggleRecordSelection(id)` | 3466 | Select/deselect in batch |
| `batchDelete()` | 3494 | Batch delete confirmation |
| `confirmBatchDelete()` | 3510 | Execute batch delete |
| `batchChangeCategory()` | 3522 | Batch recategorize |
| `confirmBatchChangeCategory(catId)` | 3558 | Execute batch recategorize |
| `deleteRecordConfirm(id)` | 3572 | Single delete confirmation |
| `undoDelete(btn)` | 3614 | Undo soft delete |
| `openEditRecord(id)` | 3639 | Edit record modal |
| `submitEditRecord(e, id)` | 3690 | Save edited record |
| `renderCategories()` | 3715 | Category management page |
| `buildCategoryTreeHTML(cats, depth)` | 3738 | Category tree HTML |
| `saveCategoryBudget(catId, month, value, type)` | 3783 | Save per-category budget |
| `mergeCategory(sourceId)` | 3810 | Start category merge |
| `confirmMergeCategory()` | 3891 | Execute category merge |
| `toggleCatItem(toggleEl)` | 3955 | Expand/collapse tree node |
| `addRootCategory()` | 3972 | Add root category |
| `addChildCategory(parentId)` | 4000 | Add child category |
| `renameCategory(id)` | 4029 | Rename category |
| `changeCategoryIcon(id)` | 4056 | Change icon |
| `changeCategoryColor(id)` | 4076 | Change color |
| `moveCategory(id)` | 4107 | Move category |
| `deleteCategoryConfirm(id)` | 4159 | Delete category |
| `buildBreadcrumb(cat)` | ~4220 | Breadcrumb for deep nesting |
| `expandAndScrollTo(catId)` | ~4245 | Expand + scroll to category |
| `editCategory(catId)` | ~4280 | Edit category modal |
| `saveCategoryEdit(catId)` | ~4350 | Save category edit |
| `getDrillCategory()` | ~4400 | Current drill category |
| `syncPieDrillBar()` | ~4405 | Update drill breadcrumb |
| `updateDrillCharts()` | ~4420 | Redraw after drill |
| `getHeatmapColor(ratio)` | ~4440 | Color for heatmap cell |
| `getDailySavingsTarget(month)` | ~4470 | Daily spendable target |
| `renderCalendarHeatmap(month)` | ~4480 | Calendar heatmap HTML |
| `showDayRecords(dateStr)` | ~4570 | Show day's records popup |
| `expandHeatmap()` | ~4630 | Expand heatmap modal |
| `expandPie()` | ~4650 | Expand pie chart modal |
| `renderExpandPieTable()` | ~4690 | Category table in pie expand |
| `toggleExpandPieRow(rowId)` | ~4800 | Toggle subcategory rows |
| `shrinkChart()` | ~4810 | Close expand modal |
| `toggleMonthCompare()` | ~4820 | Toggle month comparison |
| `drawCompareBarChart(canvasId, month)` | ~4825 | Comparison chart |
| `renderStats()` | ~4930 | Statistics page |
| `changeStatsMonth(month)` | ~5150 | Change stats month |
| `changeStatsCustom()` | ~5158 | Update custom range |
| `resetStatsDrill()` | ~5175 | Reset pie drill-down |
| `useCustomRange()` | ~5188 | Is custom range active? |
| `getChartData(month, startDate, endDate)` | ~5208 | Chart data (root aggregated) |
| `getRawChartData(month, startDate, endDate)` | ~5234 | Chart data (raw categories) |
| `drawPieChart(...)` | ~5248 | Pie chart with drill-down |
| `drawLineChart(...)` | ~5660 | Animated line chart |
| `drawBarChart(...)` | ~5820 | Bar chart with drill-down |
| `drawMonthlyChart(canvasId)` | ~5930 | 6-month trend line |
| `drawSavingsChart(canvasId)` | ~6028 | 6-month savings bars |
| `downloadChart(canvasId)` | ~6125 | Download canvas as PNG |
| `renderSettings()` | ~6140 | Settings page |
| `setSavingsType(type)` | ~6215 | Change savings mode |
| `saveBudget()` | ~6222 | Save monthly budget |
| `saveSavingsTarget()` | ~6228 | Save savings config |
| `exportJSON()` | ~6236 | Download JSON |
| `importJSON(event)` | ~6250 | Import JSON file |
| `confirmImportJSON(mode)` | ~6270 | Execute import |
| `exportCSV()` | ~6283 | Download CSV |
| `clearAllData()` | ~6295 | Clear confirmation |
| `confirmClearAll()` | ~6306 | Execute clear |
| `renderReport()` | ~6318 | Report page |
| `changeReportMonth(month)` | ~6510 | Change report month |
| `printReport()` | ~6515 | Print report |
